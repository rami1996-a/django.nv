version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Example Docker command
          command: |
            docker version
workflows:
  version: 2
  build-workflow:
    jobs:
      - build